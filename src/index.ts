import {
    EventDispatcher,
    TypedDispatcher,
    ServiceHelper,
    createServiceBuilder
} from "./builder";
import {
    DeviceInformation,
    DeviceInformationService
} from "./services/device-information";

export {
    EventDispatcher,
    TypedDispatcher,
    ServiceHelper,
    createServiceBuilder,

    DeviceInformation,
    DeviceInformationService
};
